import * as vscode from 'vscode';

export async function sugerirVersiculos(context: vscode.ExtensionContext) {
	// TODO: Implementar sugestão de versículos por tema
	vscode.window.showInformationMessage('Função: Sugerir Versículos por Tema');
}
